Please look at https://docs.nestjs.com/security/authentication

This module will be only used for login.

But, the jwt.strategy can be used in other modules for authentication (with common JWT-secret).